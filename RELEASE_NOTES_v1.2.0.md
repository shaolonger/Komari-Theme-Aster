# Aster v1.2.0

这版围绕日常巡检和排障，把状态、证据与后续分析连成一条更清楚的路径。

- 修正 VPS 对比压力评分：组内相对值不再冒充绝对风险，证据不足时不生成误导性综合结论。
- 明确 Ping 零样本、流量统计缺口和过期数据状态，避免把未知当成健康或零值。
- 统一主题时区下的统计区间，并将流量耗尽提示说明为基于当前速率的估算。
- 首页异常摘要常驻并链接到对应实例证据；手机巡检卡保持主要状态、指标和操作可读。
- 对比工作台新增“资源压力”和“线路稳定性”任务预设，可复制包含节点、指标、范围、视图及任务的可恢复链接。首页对比入口不再静默截断所选 VPS。
- Aster 工作室新增可撤销的“日常巡检”和“公开展示”首配预设，并可从 Komari 主题菜单直接打开。

后台菜单的 `redirect` 配置需 Komari 服务端高于 1.2.0。旧版本仍可通过首页右上角访问工作室。

验证：`npm run test:release-gates`，包括双后端 30/300/1000 节点浏览器门禁、1,800 次轮询 soak、工作室预设、对比 URL 恢复、类型检查、lint、RPC 合约和资源预算。
